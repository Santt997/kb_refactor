[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "kb_refactor"
version = "0.1.0"
description = "cast from kb_refactor src 2pypi"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    
]

[tool.setuptools]
packages = ["kb_refactor"]
